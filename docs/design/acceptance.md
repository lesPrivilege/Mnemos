# MX-00—06 · 施工与验收账

2026-09-16。本轮起点 `ff86d7b`；后续提交以 Git 历史为准。**施工已收口，验收有保留项；不代表全设备通过。** 输入提案与实际实现有区别，本文记录交付事实，历史分轮结果见 [执行回执](execution.md)。

## 交付映射

| 单元 / grammar | 实现与共享消费者 | 状态 |
|---|---|---|
| MX-00 | Design 索引、来源账、刊例记 57—63 | 已入账 |
| MX-01 | 独立 `design.html` / Showroom，内存 fixture，主题、减弱动态、重置、200% 文字 | 已实现；隔离测试通过 |
| MX-02 / MG-01—04、07 | Reader → CardDraftEditor / ContextDialog → confirmed card.source → SourceLens；产品与样板共享 | 已实现；失败保稿、重复句来源和焦点返回已测 |
| MX-03 / MG-05—07 | ReviewCard / RatingRail / reviewAction；显答、四档提交、预选确认、真实撤销 | 已实现；浏览器样板与真实存储集成测试分别验证 |
| MX-04 / MG-08—09 | Search、StudyTray、studyPlan、备份 ID 映射 | 已实现；按钮排序通过，实际拖动未通过验证 |
| MX-05 / MG-10 | activity 投影 → ActivityHistory → Activity / Showroom | 已实现；日期范围、零/缺失、明细、资料返回、清除已测 |
| MX-06 | 共享触控尺寸、样板样式边界、明暗与尺寸证据、本账 | 已收口；原生及独立交互验收保留 |
| MG-11 / MG-12 | 可选阅读参数预览、导入预检新界面 | 未采用，不列作已实现 |

来源沿 [source-index.md](source-index.md) 与 [grammar.md](grammar.md) 消费，不复制 donor 的业务语义。装饰左缘线已退役；对象名、字轨、留白和水平分节承担辨识。已有热图色阶保持产品基线（刊例记-31），不把提案中的色彩建议误报为本次已推广。

## 工程与数据

- `npm ci` 完成，依赖与 lockfile 无变化。
- 最终 `npm run check`：58 文件、353 测试通过；lint、collate、build 通过。collate 为既有 1 WARN、0 FAIL，未扩大豁免。`npm run collate:selftest` 通过。
- 新增回归覆盖日期包含边界、反向日期、窗口外日期、清除恢复横滚、已记录零与无记录、资料返回筛选、拖放事件排序与 current 身份保持。DOM 事件测试不等于实际指针拖放验收。
- MX-05 不新增持久数据；URL 的 `from/to` 保存范围。热图与每日明细由同一份 90 天投影筛选，窗口外不补造历史。仪表盘仍沿原有汇总读取路径；不宣称全页数据库事务快照。
- MX-02 的来源内嵌卡片，MX-04 的 typed plan 进入备份；兼容旧字段缺省和旧备份。多模块备份、卡片与活动日志仍非全局原子事务，进程在两次写入之间退出可能留下差异。复习重启沿既有卡组＋剩余数恢复合同，不声称恢复完整临时队列。
- 本轮未新增依赖、动效库、LLM、云端能力或调度语义。构建的 design entry 为 10.91 kB / gzip 5.01 kB；此数不包括共享 chunk，不冒充总加载体积。

## 实施者浏览器验收

使用真实 computer use 控制本地 in-app browser。样板数据在内存中，未向用户库注入测试记录。下表只覆盖列出的动作，不代表每场景的所有尺寸、主题排列均已穷举。

| 做了什么 | 观察到什么 | 结果 / 证据 |
|---|---|---|
| 390 深色、减弱动态，单日选择后用日期键盘改为 9/12—9/16 | 5 天与热图范围联动；阅读零值显示 0，无来源记录显示 —；反向范围报错 | 通过；[活动](evidence/mx05-390-dark-range.png) |
| 同页 200% 文字，聚焦表格并按右方向键，再清除 | 根字号 32px；页面宽/scrollWidth 均 390；表格独立横滚 80px；说明可换行；清除后日期与明细退出 | 通过；[大字](evidence/mx06-390-200pct.png)。这是文字放大，非操作系统缩放测试 |
| 834 浅色、减弱动态，制卡失败→重试→查看来源→关闭 | 输入保留；成功只产生一张样板卡；命中第二处重复句；关闭焦点回查看原文 | 通过；[失败](evidence/mx06-834-failure.png)、[来源](evidence/mx06-834-source.png) |
| 390 深色、减弱动态，显答→困难预选→确认→撤销 | 预选不提交；确认后回执；撤销恢复答面焦点和评价入口 | 通过；[复习](evidence/mx06-390-review-dark.png)。内存回执不充当持久存储证据 |
| 390 深色，多选两项→上移文档 | 文档到首位，当前项仍为卡组，焦点回文档行，回执明确 | 通过；[托盘](evidence/mx06-390-plan-dark.png) |
| 两次从独立句柄拖动文档到首行 | 自动操作后顺序未变化；增加原生 dataTransfer payload 后仍未获得端到端通过 | 未通过验证；按钮替代可用，不据此断言全部浏览器拖放失效 |
| 1440 浅色活动单日 | 表格四列可见，页面宽/scrollWidth 均 1440 | 通过；[宽屏](evidence/mx06-1440-activity-light.png) |

书影页脚中的 `b76af1c` 为此前启动的 Vite 进程编译时版本标记，热更新并不刷新该标记；这些书影是本轮工作树的实时画面，不能把页脚当作其源码提交 ID。

## 独立审校与保留项

Luna 独立只读源码及书影审校另记于执行回执；实施者的交互事实不冒充独立操作。Luna 浏览器连接多次超时，独立交互保持未执行。

ADB 设备清单为空，因此 Android Back、系统文本选择柄、触觉、WebView 长列表和切后台恢复均未执行。遵循 [施工单 §5.2](implementation.md#52-可见行为)，无设备项目交下一轮补验，不编造通过。实际拖放还需在有完整指针事件的浏览器/设备复测。

样板可在 `/design.html` 重置或刷新，生产改动可按对应提交回退；不通过删除用户数据复位。今后的最小补验是：独立浏览器完成同样操作、实际拖放，以及 Android 真机生命周期矩阵。以上保留项不以“施工完成”销账。
