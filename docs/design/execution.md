# MX-00／MX-01 执行回执

2026-09-16 · 源码基线 `b76af1c8ead40cd7e8e31ad3c55f47f6d1c02e97`。实现提交：`d9a89a1` 数学渲染、`27025a8` 左缘衬线退役、`aaaf79d` 独立样板。
输入为 Desktop/Mnemos-Design-Showroom-20260916；其中历史授权描述、候选合同与未实现方案保持输入性质。本轮按用户“串行施工；Luna explore、Astra 裁决实现、保留视觉 computer use”执行。

## 当前交付

- MX-00：Design 主稿、grammar、施工单和来源账入库，README／CLAUDE／roadmap 设短指针，刊例记-57 记录本轮适用范围。
- MX-01：`design.html` 独立启动，Vite 双入口；`src/design` 内存场景，不挂载 App、不读取用户库。共享 `ReaderPanels`、`renderDoc`、字体和 tokens。
- 阅读正文 → 系统选文或段落按钮 → 摘录 → 草稿 → 保存／失败重试 → 查看段落。样板卡组是内存标签；来源定位是本次 fixture 的段落索引。
- 空资料、长标题、重复摘句、公式定位、无笔记、有笔记、保存失败、来源失效可选；主题、单栏／并置、减弱动态、重置可操作。
- MG-02／03／04／07 仅为状态骨架；选区旁避碰工具条、跨页面对象转场、持久 source link、复习与撤销尚属 MX-02／03，不宣称完成。

## 相邻修正与安全

共享高亮日期文字从 ink-4 归 ink-3。Computer use 发现 KaTeX 矩阵数字重叠：原清理器删掉生成内容中的定位 style。修为先清理用户 Markdown，再回填 `trust:false` 的 KaTeX 生成片段；用户 HTML style／事件仍被删除。数学片段 Map 只包围同步 marked.parse；若改为异步扩展须先改为每次渲染独立上下文。

无新增依赖或持久字段，无备份合同变化。设计入口独立 JS 约 9 kB（gzip 约 4 kB，本轮构建）；本值不是与旧构建的差分，公共字体和渲染代码由 Vite 共享。

## 工程证据

- `npm run check`：lint、48 个文件／316 项测试、collate、双入口构建通过；包含追加 TeX 反例、公式初始定位与左缘衬线递修。
- `npm run collate:selftest`：14/14 探测器触发，PASS；现有基线 1 WARN、0 新 FAIL。
- 行为测试覆盖失败保留与重试、场景重置、Storage／IndexedDB 零调用、第二处重复句、来源缺失保留摘句、数学定位样式和不可信 HTML。
- 本机已存在 node_modules，本轮未重新执行 npm ci、未变更 lockfile。

## Computer use 观察（实施者）

| 做了什么 | 观察到什么 | 结论 |
|---|---|---|
| 834×1112 浅色并置，编辑问题后触发保存失败 | 编辑文字保留；错误与重试按钮在局部面中 | 通过 |
| 重试保存，再查看原文 | 成功回执；焦点位于“回到原来的问题”之后的第二处重复句 | 通过，仅 fixture 内 |
| 390×844 深色、减弱动态 | 单栏完整；scrollWidth＝390；已保存卡片仍在 | 通过 |
| 重置场景 | 卡片消失，恢复种子摘录；控件偏好保留 | 通过 |
| 320×760 长标题 | 标题换行，无横向溢出，scrollWidth＝320 | 通过 |
| 复核矩阵与主按钮 | 矩阵两行两列恢复；主按钮悬停保持墨底纸字 | 通过 |
| 真机 Back／系统选择柄／触觉、IME、200%文字、forced-colors、录像 | 本轮未执行 | 未执行 |

书影由真实浏览器产生，均基于上述 source HEAD 加本轮工作树（最终源码归于 aaaf79d；开发服务在基线启动，页脚构建戳仍显示 b76af1c）；fixture 为 `src/design/fixtures/reading.md`。无录屏，不代替原生设备验收。

- [834 浅色／正常动态／保存失败](evidence/mx01-834-light-failed.png)
- [390 深色／减弱动态／保存后](evidence/mx01-390-dark-reduced.png)
- [320 浅色／长标题](evidence/mx01-320-light-long.png)

## 独立审校

Luna 只读检查确认入口隔离、失败草稿、Vite 双入口与日期 token 修正；针对测试 4 项和 eslint 通过。初审指出本执行回执缺失（已补），公式场景无独立落点（已补到公式段落），恶意 TeX 反例缺失（已补）。持久来源身份与异步渲染风险留明，不扩大首单结论。独立浏览器审校仅完成浅色初始页：确认控件、正文、空摘录、公式段落与 MX-02 边界文案可见。后续 Chrome 缩放菜单异常，computer-use 新建/操作调用超时；834／390、深色、失败重试和有内容的去左线状态未获独立视觉通过。实施者随后恢复现有 in-app browser 并补录最终截图；这些截图不替代独立验收。

用户预览后要求取消左侧衬线；记-58 已移除共享摘录、Markdown 引用和导入预览的装饰左线，改为对象名、引用内收与真实序号。上述初版书影早于该调整，保留作过程证据；最终书影如下（实施者 computer use，版本 aaaf79d）：

- [834 浅色／失败／取消左缘衬线](evidence/mx01-final-834.png)
- [390 深色／减弱动态／保存成功](evidence/mx01-final-390.png)

最终移动视口复核 innerWidth＝scrollWidth＝390，visualViewport.scale＝1；测试后的缩放和视口覆盖已恢复。

## 下一串行单元

**退出状态**：MX-00 入账完成；MX-01 实现与工程门通过，独立视觉矩阵未完成，暂不写定或推广。

MX-02：先读取最新 Reader／Import／卡片 ID／备份合同，修重复句定位，再实现生产局部制卡与持久来源关联。MX-03…06 尚未施工。当前工作可通过独立提交撤回；不修改用户数据，不 push。


# MX-02 执行回执

2026-09-16 · 起始 HEAD `19c96db`，本节覆盖其后 MX-02 工作树。裁定 59；无新增依赖。

## 实现与数据

- Reader 真实选区记录 DOM 字符位置；重复句不再用首次字符串匹配。共享选区工具条、草稿编辑器、来源查看面由生产与样板共同消费。
- 草稿保存等待 IndexedDB 提交，失败保留输入，重复提交复用卡片 ID。来源内嵌在卡片同一事务内；源文本 SHA-256 与位置同时相符才精确回跳，改文提供候选，失效保留摘句。
- Browse／Review 保持卡片挂载，关闭来源恢复触发控件焦点；原生 Back 优先关闭局部面。未改变 SM-2。
- 备份合并映射文档／摘录身份，冲突不覆盖旧正文，重复恢复复用相同对象；卡片恢复等待事务。跨模块恢复仍非全局原子，失败可留下先前写入，详见 backup-format.md。
- 左缘装饰线继续退役：使用摘录对象名、段间留白、水平分隔和原文选区。结构分栏边界保留。

## 验证

- `npm ci` 完成，lockfile 未变更。`npm run check`：51 文件／327 测试通过，lint、collate、双入口构建通过；collate 现有 1 WARN、0 FAIL。`collate:selftest` 14/14 通过。
- 真 IDB 适配的集成测试覆盖 Reader 第二处重复句→保存失败→重试→Browse→来源→返回；另覆盖重启水合、备份冲突与重复合并、无映射来源、提交失败不变更缓存。
- 实施者 computer use：834 浅色保存失败保留输入、重试与第二处来源；390 深色减弱动态保存→来源→返回同一卡片，焦点回到“查看原文”。来源打开时背景 inert；修复延迟初始焦点抢走原文定位的问题。
- [834 来源定位](evidence/mx02-source-834.png) · [390 深色来源定位](evidence/mx02-source-390-dark.png)。开发服务页脚仍显示启动时 b76af1c，截图对应本节工作树。
- Android 真机、IME、200%文字、forced-colors 尚未执行。上述截图和交互为实施者证据，不替代独立交互验收。

Luna 独立源码初审的三项问题（重复合并、缺摘录映射、恢复未等待事务）已修复并加入回归；最终增量审校确认无新阻断项；另指出高亮上下文字段未参与等价判断，已补齐比较字段。独立审校未使用浏览器、未执行测试。MX-03…06 尚未施工，programme 保持进行中。

## MX-03 · 显答首切片

起始 HEAD `d896748`。共享 ReviewCard 改为唯一题干、答案原位展开、显式显示／收起按钮。显示后焦点进入答案，收起后回到按钮；不缩放公式。样板新增“复习显答”，复用产品组件。

`npm run check`：51 文件／326 测试，lint、collate、build 通过（两个旧 class 断言测试替换为一个行为测试）；无依赖变化。Luna 对本切片源码审校无阻断。实施者 computer use 验证 390 浅色显示／收起、公式完整、答案焦点与按钮返回；[书影](evidence/mx03-reveal-390.png)。题干坐标的浏览器读数因一次超时未取得，不将其计为测量通过。

MX-03 尚未完成：Luna 展开确认成熟卡 Again 撤销未还原持久状态、初始空队列误判完成、重排沿用旧卡快照，以及中断计数和撤销日志缺口。下一切片先修复这些业务问题，再接离散评价预选和常驻撤销；保持原 quality 与 SM-2 公式。

## MX-03 · 评价与真实撤销

起始 HEAD `3eee299`；quality 与 SM-2 函数未改。共享 RatingRail 提供四档直接按钮，以及原生 range 预选＋显式确认；拖动不写入，删除动画延迟提交与整卡左右滑动直接评分。

- `reviewAction` 计划学习／重排分支，确认 IndexedDB 后记录带 ID 的事件；失败补偿；二次补偿失败明确说明未完整恢复，并阻止继续评分。跨卡片与事件存储不是全局原子事务，进程在两次写入间退出仍可能产生差异，尚无崩溃恢复日志。
- 评价入口同步锁防重复；保存中禁用返回／收藏／来源，native Back 通过 guard 拦截。初始载入不再当作完成；中断计数使用实际剩余队列。
- 撤销恢复 SM-2、学习 passes、队列位置、统计与对应事件；从当前库合并非评分字段以保留收藏变化。成熟 Again 重排携带新卡片状态；暂停卡不重排。完成项数与评价次数分别显示。
- 常驻撤销按钮独立于限时回执；预选 summary 的键盘不会同时翻卡。正常触觉保留平台适配，浏览器无触觉；无新增动效或依赖。

工程：`npm run check` 54 文件／338 测试通过，lint／collate／build 通过，基线 1 WARN、0 FAIL。新增测试覆盖成熟 Again、leech／撤销、learning 分支、末卡撤销、重复提交、事件删除、确认写失败、两层补偿失败、中断计数、保存中禁用控件与 summary 键盘。

Luna 两轮只读审校：前述 P1 均关闭；最后两项 P2（撤销后的收藏 UI、换 deck 的 faulted）已修。实施者 computer use：390 浅色样板显答→预选困难（不提交）→确认→回执→撤销→答案焦点，使用共享 ReviewCard／RatingRail。[书影](evidence/mx03-rating-390.png)。样板回执仅在内存；真实存储逆操作由集成测试验证，尚未获独立生产浏览器／原生设备交互验收。

后续仍须完成 MX-04／05／06、跨入口视觉矩阵与设备限制登记。当前 session 恢复仍沿用既有“卡组＋剩余数，重新取到期卡”合同，未宣称重启还原完整临时队列。
